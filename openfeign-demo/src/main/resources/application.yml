eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/,http://localhost:8763/eureka/

server:
  port: 6060

spring:
  application:
    name: feign-client

feign:
  compression:
    request:
      enabled: true
    response:
      enabled: true